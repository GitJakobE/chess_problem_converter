name: opencv
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - poppler
  - matplotlib=3.5.1
  - pip=22.0.3
  - pip:
      - numpy==1.22.4
      - pandas==1.3.4
      - opencv-python
      - opencv-contrib-python
      - pdf2image
      - pytest
prefix: condaenv-opencv
